// The public marketing site. Separate origin from the app, so links to it
// open outside the TWA's scope in a Custom Tab — which is intended.
export const MARKETING_URL = 'https://info.revisionworks.in';
